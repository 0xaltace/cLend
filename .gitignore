# Secrets — never commit
.env
.env.*
!.env.example
**/.env
**/.env.*

# Local tooling / AI skill bundle (not part of the product)
.claude/

# Local-only dev scripts & archived files — kept on disk, never pushed
xyz/

# Editor settings — keep local, don't publish
.vscode/

# fhevm
fhevmTemp

# Dependencies
node_modules/
.pnp.*

# Build artifacts
artifacts/
build/
cache/
coverage/
coverage.json
.coverage_artifacts
.coverage_cache
.coverage_contracts
types/
typechain-types/
deployments/
dist/
.next/
tmp/

# Misc
*.log
.DS_Store
pnpm-lock.yaml
yarn.lock
